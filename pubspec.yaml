name: projectmanagementflutter
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  http: ^1.2.1
  flutter:
    sdk: flutter
  url_strategy: ^0.2.0
  flutter_web_plugins:
    sdk: flutter


  cupertino_icons: ^1.0.6
  dio: ^5.4.2+1
  go_router: ^13.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/


